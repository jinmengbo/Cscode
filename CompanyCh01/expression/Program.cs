using System;

namespace expression
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
             1. 表达式体方法
                int Foo (int x) {renturn x*2}
                简化后,可以用表达式体方法简洁地表示，用双箭头来取代花括号和return关键字
                int Foo (int x) => x * 2;
             2. 表示式也可以用void 作为返回类型，如果我们这个表达式没有返回的类型，可以用void来代替。
                如下:
                void Foo(int x) => Console.WriteLine(x);
             */
        }
    }
}
